import React from 'react'

interface PlayerProps {}

// eslint-disable-next-line no-empty-pattern
const Player: React.FC<PlayerProps> = ({}) => {
  return (
    <section>Player container</section>
  )
}

export default Player